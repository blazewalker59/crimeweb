node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.dev.vars
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

# Claude Code agent worktrees and local settings
.claude/worktrees/
.claude/settings.local.json
# Symlink into node_modules, regenerated by `vp config` on every install
.claude/skills/
