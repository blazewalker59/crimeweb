node_modules
.DS_Store
dist
dist-ssr
*.local
count.txt
.env
.dev.vars
.nitro
.tanstack
.wrangler
.output
.vinxi
todos.json

# Claude Code: agent worktrees, local settings, and everything `vp config`
# generates on install (settings.json, plus a skills symlink into node_modules).
# Un-ignore a specific file if a shared, committed setting is ever wanted.
.claude/
